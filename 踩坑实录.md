# 踩坑实录

> 项目里真踩过、真修复的坑。每条按「现象 → 排查 → 根因 → 修复 → 教训」记录，
> 能复现的附最小复现代码。写给未来的自己，也当面试素材。

---

## 坑 1：handoff 转接时，output_guard 和专家节点并发抢跑（LangGraph 双触发）

**一句话版**：节点返回 `Command(goto=X)` 并不会"顶替"它的静态边——两条路会同时触发，
质检节点拿着旧 state 和真正的接班专家并发抢跑，审了个空气。

### 现象

Langfuse trace（`81250dd38de68aef8239fc2bda7e55aa`）里 `output_guard` 出现了多次，
且入参对不上。最初只看到两个 span：

```
presale_expert  3.41s
└─ tools → transfer_to_aftersale_expert      ← 售前交接给售后
aftersale_expert  5.91s
└─ model → ChatOpenAI  5.90s                 ← 售后答题
output_guard  0.00s                           ← 没有 LLM 子调用，秒过
output_guard  2.94s                           ← 带 RunnableSequence（LLM 审查）
```

后来通过 Langfuse 公开 API（`GET /api/public/traces/{traceId}`）拉到全量 27 个
span 的毫秒级时间戳，完整时间轴是（截取关键部分）：

```
06.181  ┬ output_guard #1 (0.003s)   ← 幻影护栏：入参 7 条消息，无售后答复
        └ aftersale_expert #1 (5.91s) ← 同一毫秒并发起跑，入参完全相同
12.095  output_guard #2 (2.94s)      ← 入参 8 条，带售后答复，LLM 审查后打回
15.035  aftersale_expert #2 (10.88s) ← 打回重答（这次调了 query_refund_policy）
25.917  output_guard #3 (0.001s)     ← 轨迹校验通过，放行 END
```

铁证在 **16:20:06.181 这一毫秒**：幻影护栏和售后专家【同时】起跑，且两者的
输入 state 一模一样（消息数、内容完全相同）——它们是从同一个超步并发分叉
出去的两条分支。幻影护栏拿不到售后答复（此刻答复还不存在），`_final_answer`
返回 None，空 update 秒放 END——审了个空气。

### 排查

1. 先排除了一个错误假设：同名 span 不是 checkpointer 状态快照（checkpoint 落库不产生 span，
   trace 只记录 node/runnable 的执行）。同名嵌套且等时长的才是子图包装（如
   `aftersale_expert` 套 `aftersale_expert`），而多个 guard span **互不嵌套、耗时不同、
   入参不同**，说明节点真被执行了多次。
2. 查 checkpoints 库想还原现场，thread 已不在库（UI 会话）。一度只能靠截图推断，
   还把两个 guard 的先后顺序搞反过一次——**教训：截图会裁掉后半截链路、UI 同毫秒
   并列的 span 顺序不可靠，必须用 API 拉原始时间戳**。最终用 Langfuse 公开 API
   拿到全量 span 的 startTime/endTime/输入输出，同一毫秒并发起跑 + 入参完全相同，
   并发分叉实锤。
3. 写了一个 20 行的零 LLM 复现图，复刻线上接线：

```python
# 父图：a 是子图节点，内部返回 Command(goto="b", graph=PARENT)
#      a 和 b 各有一条静态边指向 g（复刻 expert → output_guard）
p.add_edge(START, "a")
p.add_edge("a", "g")   # ← 线上写法：专家到护栏的静态边
p.add_edge("b", "g")
p.add_edge("g", END)

graph.invoke({"messages": [("user", "hi")]})
# 输出：
# g 执行次数: 2
#   第1次入参: ['hi', '子图交接信令']            ← 没有 b 的答复
#   第2次入参: ['hi', '子图交接信令', 'b 的最终答复']
```

复现结果和 trace 完全对上：g 跑两次，第一次入参没有最终答复。

### 根因

直觉上以为"节点返回 `Command(goto=X)` 就是改道去 X，原来的边就不走了"——**错**。
在 LangGraph（实测版本）里，子图通过 tool 级 `Command(goto=..., graph=Command.PARENT)`
交接时，父图层面：

- Command 的 goto 触发目标节点 `b` ✅
- 节点 `a` 的静态边 `a→g` **照样触发** ✅（不会被 Command 抑制）

于是 `b`（接班专家）和 `g`（护栏）在同一个超步里**并发**执行，g 拿到的是分叉时刻的
旧 state。等 b 答完，`b→g` 边再触发第二次 g——这才是带最终答复的那次。

> 注意和常规认知的差异：节点**函数体直接返回** `Command(goto)` 时 goto 会替代静态边；
> 但**子图内 tool 级 Command 冒泡到父图**这条路径上，静态边不 suppressed。behavior
> 不一致，官方文档没明说，属于"不踩不知道"的实现细节。

### 修复

专家的出口从静态边改成**条件边**（`graph.py` 的 `_expert_exit`）：handoff 在途时本边收工
走 END，把舞台让给 Command 的目标；正常答完才进护栏。

```python
def _expert_exit(state: State) -> str:
    """handoff 在途 → END；正常答完 → output_guard。"""
    last = state["messages"][-1]
    # handoff 生效时子图被拦停，最后一条消息是 transfer_to_* 的 ToolMessage
    if (getattr(last, "type", "") == "tool"
            and str(getattr(last, "name", "")).startswith("transfer_to_")):
        return END
    return "output_guard"

for name in EXPERTS:
    builder.add_conditional_edges(
        name, _expert_exit, {"output_guard": "output_guard", END: END}
    )
```

判定依据：handoff 生效后子图立刻停跑，父图 state 的最后一条消息一定是
`transfer_to_*` 的 ToolMessage；正常答完则是专家的最终 AIMessage。

修复后复现脚本断言：**g 只跑 1 次，且入参带最终答复**。PASS。

### 危害不止"trace 脏"

并发抢跑的那次 guard 是个定时炸弹：

- 它审的是旧 state，万一交班专家留了一句带内容的答复（比如先说了句"我帮您转接"），
  护栏会对**错误的专家、错误的答复**做 LLM 质检——白烧一次 judge 调用
- 更糟的情况：它判了"打回"，`goto active_agent` 把流程拽回正在接班的专家节点，
  和并发的正常流程撞车，状态就彻底乱了

### 教训 / 面试怎么讲

1. **图框架里"边"和"Command 跳转"是两套触发机制，叠加时必须先验证会不会双触发**，
   不能拿直觉当文档。通用原则：动态路由（Command）和静态接线（edge）覆盖同一条路径时，
   一边必须让位——用条件边把"什么时候走哪条"写成显式代码。
2. **trace 是 AI 系统的"日志 + 性能分析器"**：这个 bug 没有报错、没有异常，功能上
   "看起来能跑"，唯一的破绽就是 trace 里两个同名 span 的入参差异。看 trace 不能只
   看调用链形状，要**点开 span 对入参/出参**。
3. **怀疑框架行为时，写最小复现**，别在业务图上猜：20 行零 LLM 的脚本就把机制钉死了，
   还能当修复后的回归断言。
4. 面试话术参考："我在多 Agent 客服系统里发现过一个 LangGraph 的并发坑——子图
   handoff 的 Command 和父图静态边双触发，导致质检节点拿旧状态并发抢跑。我通过
   trace 的 span 入参差异定位、最小复现确认框架行为，最后用条件边把互斥关系显式化。
   这类问题功能测试测不出来，必须靠可观测性 + 状态级断言。"

---

## 坑 2：售后专家不查 RAG 库就敢答政策（无据出数）

**一句话版**：模型拿着对话上下文里的一点印象直接回答退换货政策，一次检索工具都没调——
在客服场景这就是"编造政策"，软约束（prompt 红线）没拦住，得从指令和工具描述两头堵。

### 现象

同一个 trace 里，售后专家的**第一次**执行，内层结构是这样的：

```
aftersale_expert #1  5.91s
└─ model → ChatOpenAI  5.90s     ← 只有一次模型调用，没有 tools 子节点
```

售前转过来的用户问的是退换货政策类问题，售后专家**一次 `query_refund_policy` 都没调**，
直接凭对话上下文和模型记忆给了政策答复。知识库就在眼前，它没去翻。

（最初看截图以为故事到此为止——护栏没拦住。拉全量 trace 才发现后半截：
护栏其实拦了，见下。）

### 根因

两个因素叠加：

1. **上下文给了它"我会"的错觉**：售前接待时聊过相关话题，消息历史里有只言片语，
   模型觉得"够答了"，跳过取证直接生成。这是 LLM 的经典毛病——有上下文就不爱调工具。
2. **工具描述形同虚设**：`query_refund_policy` 的描述只有一句"查询退换货政策说明。"——
   没说什么时候**必须**调，模型做工具选择时拿不到强信号。
   （但描述烂不是不调用的理由，prompt 里本来就写着用它，是模型自己偷了懒。）

**护栏这次其实拦住了，完整自愈链路是这样的**（全量 trace 时间轴）：

1. 售后第一次答复带数字（政策时限类）+ 本轮零业务工具调用 → 触发轨迹校验，
   交 LLM 审查（`output_guard` 2.94s 那次，子 span 是 RunnableSequence 判官流水线）
2. judge 对照全会话工具证据池，判 **VIOLATION：「全会话无任何检索依据」**
   → 护栏 RemoveMessage 摘掉原答复 + 隐身 SystemMessage 指出违规点 + 打回售后重答
3. 售后**第二次**执行（10.88s）乖乖调了 `query_refund_policy`（18.4s 处的 tools
   span），拿着政策原文重写答复
4. 第三次 guard：本轮有业务工具调用，轨迹校验直接过（0.001s），放行 END

反幻觉体系（轨迹校验 + LLM 审查 + 打回重答）端到端跑通了一次。**但代价肉疼**：
这一轮总共 23.16s，其中白答 5.91s + judge 2.94s + 重答 10.88s ≈ **20s 都是为
"第一次没查 RAG"交的学费**。事后兜底接住了是底线，事前让它第一次就查才是
省钱——所以 prompt 硬性条款和工具描述照样要补。

### 修复

护栏的事后兜底已验证有效，要补的是**事前预防**——目标是让第一次就查，
省掉"白答 + judge + 重答"约 20s 的学费。两头堵，`prompts.py` + `rag/retriever.py`：

1. **AFTERSALE_PROMPT 加硬性条款**（把"什么时候必须查"写成规则，不是建议）：

```
⚠️ 凡回答退换货条件、退款流程、到账时限，必须先 query_refund_policy 取证再答；
禁止凭印象/常识直接给政策结论（唯一例外：本会话你刚查过、内容足以覆盖该问题）
```

   例外条款是刻意的——和护栏的"全会话证据池"设计对齐，给合法的跨轮引用留口子，
   不然会逼出"每轮重复查同一个政策"的浪费。

2. **工具描述改成带触发条件的指令式**：

```python
@tool
def query_refund_policy() -> str:
    """查询退换货政策、退款流程、到账时限、换货条件的官方说明。
    回答任何退货/退款/换货规则类问题前，必须先调用本工具取证，禁止凭记忆作答。"""
```

   工具描述是模型做工具选择时读到的"使用说明书"，写清"什么场景必须用"比写
   "这是什么"重要得多。

### 教训 / 面试怎么讲

1. **"模型没调工具"本身就是一类缺陷**，而且是最隐蔽的一类：答复看着通顺、像模像样，
   实际无据出数。银行金融测试里"结果对但过程违规"是高危，AI Agent 测试同理——
   **断言要打到轨迹层**（该调的工具调没调），不能只断言最终文本。
2. **软约束（prompt 红线）是概率约束，压不住所有样本；但机械兜底是真的能救命**。
   本案例里"轨迹校验 + LLM 审查 + 打回重答"把无据答复拦下并逼出了正确行为——
   防线要分层：prompt 硬性条款（事前引导，最便宜）→ 工具描述指令化（选择时干预）
   → 护栏事后兜底（最贵但最可靠）。本例兜底一次的成本 ≈ 20s 延迟 + 两次 LLM 调用，
   而事前条款的成本≈零。越靠前越便宜，越靠后越保险，一层都不能少。
3. **工具描述是 prompt 的一部分**，而且是被低估的一部分——它直接参与模型的工具选择
   决策。"写得垃圾的描述"不是文档问题，是功能缺陷。
4. trace 断言 checklist 参考：售后政策类问题 → trace 里 aftersale 节点下必须挂
   `tools → query_refund_policy`；没带就是缺陷，不管答案"看起来"对不对。

---

## 坑 3：FAQ 问答对和成篇文档混灌一张向量表，检索互相污染

**一句话版**：faq / products / refund_policy 三种数据类型灌进同一套
`data_kb_vectors` / `data_kb_docstore`，靠 `metadata->>'source'` 过滤假装隔离——
向量空间和 BM25 词频统计实际上全是混的。

### 现象

查库实锤（2026-09-25）：`data_kb_vectors` 里 `faq.md` 70 条、`products.md` 31 条、
`refund_policy.md` 15 条共存；BM25 索引 `kb_bm25_idx` 直接建在这张混表上。

### 根因

检索隔离只做到了 SQL 的 WHERE 层，没做到数据层：

1. **向量层**：问句（短文本）和条目段落（长文本）相似度尺度不同，混在同一个
   HNSW 索引里，metadata 过滤是检索后/检索中过滤，top_k 名额先被异类节点挤占。
2. **BM25 层更伤**：IDF 是全表统计，faq 里的高频问句词会拉低/抬高成篇文档里
   同一个词的权重——这不是过滤能解决的，语料混了统计就混了。

### 修复

一文档一套独立三件套（丽姐拍的板）：`kb_product_*` / `kb_policy_*` / `kb_qa_*`
各自 vectors + docstore + indexstore + 独立 BM25 索引，路由登记在
`ingest._CORPUS_ROUTES`；检索侧 source 过滤整层删除，工具 1:1 打自己的表。
旧混表整表 DROP 重灌（`db/cleanup_kb_split.sql`）。
顺带发现：llama-index 的 PostgresKVStore 落库表名也带 `data_` 前缀
（docstore 实际是 `data_kb_docstore`），清旧表时差点漏掉。

### 教训 / 面试怎么讲

1. **"逻辑隔离"不等于"物理隔离"**：metadata 过滤只能过滤结果，隔离不了索引
   统计（HNSW 图结构、BM25 的 IDF）。数据分布不同的语料（问句 vs 段落）必须分库。
2. **判断要不要拆表的标准**：看检索单位。本项目一个工具只打一个文档，
   存储按文档 1:1 对齐最直白；如果未来要做跨文档联合检索，才需要回到同表 + 过滤。
3. 排查 RAG 问题先查库再猜链：一句 `GROUP BY metadata_->>'source'` 就把
   "混搭污染"从猜测变成实锤——RAG 系统的表也是测试对象，别只测 API。

---

## 坑 4：评测跑批挂进 web 服务进程，被 ASGI 阻塞检测器团灭（评测要独立于 LangGraph 循环之外）

**一句话版**：把 Langfuse 评测跑批塞进 langgraph dev 的后台任务里，被它的阻塞调用
检测器逮个正着，`BlockingError` 刷屏。评测执行器必须独立于被测系统进程之外——
这是测试通则，不是 LangGraph 的特殊癖好。

### 现象

给 Langfuse 的 Remote Experiment（UI 点 Run → webhook 触发本地跑批）接端点，
第一版用 FastAPI `BackgroundTasks` 在 langgraph dev 进程里跑。一点 Run，
`BlockingError("Blocking call to time.sleep")` 大量刷屏，trace 里 `experiment-item-run`
批量挂掉（traceId `3cbc1c7a3fafd30e9160a28663dfacf4` 那批），堆栈指向 OpenAI
SDK 的同步 HTTP 客户端和 runner 里的 `time.sleep` 重试。

### 根因

两层，表层是框架机制，深层是架构越界：

1. **表层——langgraph dev 的阻塞检测器**：ASGI 服务最怕事件循环被同步调用堵死，
   所以 dev server 给"非图执行路径"装了探针，逮到 `time.sleep` / 同步 HTTP 直接
   抛异常。平时 UI 聊天不炸，是因为图执行这条路被官方"盖了放行章"；而我们挂的
   BackgroundTasks 没章——anyio 线程池还会把上下文变量带进线程，检测器照样认得。
2. **深层——批量住进了联机进程**：评测跑批是 20 分钟级的批处理作业，却寄生在
   对外服务的 web 进程里。就算检测器放行（`--allow-blocking`），事件循环被堵、
   dev server 热重载杀跑批、跑批崩了连坐前端对话——颗颗都是雷。

### 修复

webhook 端点改成**拉独立子进程**跑 runner（`experiment_webhook.py` 的
`_spawn_experiment`）：收触发 → 秒回 200 → `subprocess.Popen` 起
`python -m customer_service.analytics.langfuse_runner`，日志落
`logs/eval/<run名>.log`，`CREATE_NEW_PROCESS_GROUP` 保证 dev server 重启
不杀跑批。跑批代码一行没动——CLI 和 webhook 两条触发路径共用一个执行器。

### 教训 / 面试怎么讲

1. **评测 harness 独立于被测系统，是测试通则不是框架癖好**：你不会把测试脚本
   部署进被测系统的进程——AI Agent 同理。本项目的隔离边界做得很彻底：
   数据集在平台（Langfuse）、执行器独立进程、每条用例独立 thread_id
   （checkpoint 互不污染）、评分器与图代码分离、trace 用 `run:<批次>` tag
   归堆。被测系统换成任何实现，harness 一行不用改。
2. **"LangGraph 循环之外"还有一层意思**：评测驱动的是脚本化的多轮输入
   （确定性剧本），不是被测系统的 ReAct 循环——harness 从外部喂消息、收结果、
   打分数，图的内部循环对评测完全透明。这正是"黑盒被测、白盒断言"的结构：
   行为黑盒驱动，轨迹白盒断言（该调的工具调没调、路由对不对）。
3. **同步改异步治的是另一条路**：图内同步 PG/LLM 调用改异步，治的是联机路径
   的并发上限（技术债，记账 P9）；但它救不了批量任务——20 分钟的作业就算全
   异步也不该住 web 进程里。**批量不落联机**，银行系统的老规矩，AI 时代照用。
4. 排查口诀：`--allow-blocking` 是麻药不是药——它能让报错消失，但事件循环
   被堵的事实没变，同进程的其他请求都在排队陪跑。

### 附记：转人工（interrupt/resume）为什么从不踩这个雷

常被拿来反问："转人工回调不也是 HTTP 回来吗，怎么没事？"——因为它走的
**不是自建 webhook**：`human_node` 的 `interrupt()` 挂起在 langgraph dev 内部，
坐席回话走 LangGraph 原生 `POST /threads/{id}/runs`（command resume）——
平台自己的路由、自己的 run 执行器。双保险：① 路径是"图执行"官道，有阻塞
放行章；② resume 后只拼 AIMessage 就 `goto=END`，一次 LLM/阻塞调用都没有。
**interrupt/resume 是 LangGraph 的内政，webhook 才是外事**——内政走官道，
外事才轮到自己操心检测器。

---

## 坑 5：评分规则自己就是缺陷重灾区（裁判误判 guard 用例 + comment 上浮规则盲区）

**一句话版**：跑完评测发现"低分没备注、有备注的又没犯错"——查到最后，
出问题的不是被测系统，是**评分器自己的规则**。评分规则也是代码，也要被测。

### 现象

某轮跑批（2026-09-25 19:46）后，Experiments 页两个疑点：

1. `a461d626`（anaphora-product）：relevance=3 且带 comment——"第2轮：答非所问
   且编造价格：用户未指定商品型号，客服却擅自给出K87/K75两款键盘价格"。
   但这轮系统行为是**符合用例设计的**（turn_pass_rate=1，机械要点全中）。
2. `18367b91`（guard-recite-context）：relevance=1 的极端低分，却**没有任何**
   comment，失败原因完全沉默。

### 根因（一层比一层深）

1. **comment 上浮规则有盲区**：旧规则只看"单轮 rel+acc ≤ 4"。guard 用例
   rel=1 + acc=5 = 6，绕过规则——单维极端崩塌被另一维的高分掩护了。
2. **裁判不知道用例意图，系统性冤枉 guard 用例**：guard 用例期望的是
   input_guard 拦截（拒绝服务），系统正确拦了；但 relevance 裁判只看到
   "用户要复述内容、客服没给"，判 1 分。裁判的证据投喂里从来没有
   "本用例期望拒绝"这个上下文。
3. **裁判与用例设计打架（a461d626 案）**：该用例的验收策略被设计者刻意放在
   机械层（`=K87` `=K75` 都报价即算澄清式应答），软要点故意留空。结果裁判
   没收到验收标准，自由发挥，把设计内的合法策略判成"编造价格"——
   **机械侧说对、裁判侧说错，信谁？信机械侧**。
4. **顺手牵出的第四只老鼠**：Langfuse 托管 Hallucination 裁判的变量映射对不上
   本项目 task 的结构化输出（`{case_name, turns}`），它读到的 generation 恒为
   空——每条 comment 都是 "The generation is empty"，每轮烧钱评空气。

### 修复

- `judge_evaluator`：`expect_agent == "input_guard"` 的轮**不送裁判**
  （拦截即满分，relevance 对拒绝话术无意义）
- comment 上浮规则加"或"分支：`rel + acc ≤ 4` **或** `min(rel, acc) ≤ 2`，
  单维崩塌藏不住
- 托管 Hallucination：变量映射修不好就先关（它的 0 分零信息量）

### 教训 / 面试怎么讲

1. **评分规则是被测系统的一部分，也要过评审**：评测代码的 if/else（上浮阈值、
   跳过条件）直接决定"什么叫失败"，它们本身的 bug 比被测系统的 bug 更隐蔽——
   因为它不会产生异常，只会产生**错误的结论**。
2. **改评分规则 = 换基线**：guard 用例从此没有 judge 分（跳过而非满分），
   新旧规则的分数不能直接比。每次改评分逻辑，run_name 标记，历史对比划断代线。
3. **裁判的证据投喂是设计活**：裁判判错多半不是模型笨，是你给它的上下文里
   缺了"这题的验收标准是什么"。判官不知案情，只能自由心证。
4. **分数异常先怀疑尺子，再怀疑对象**：低分没 comment、有 comment 的没犯错——
   这种"数据和直觉打架"的信号，八成是测量系统自己的问题。
5. 排查方法论复用：Langfuse 公开 API（traces/{id} 带 scores）直接把每条用例的
   分数和 comment 拉出来对——**别在 UI 上一页页点，API 取证又快又全**。
